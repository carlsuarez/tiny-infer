[workspace]
members = ["engine", "llama", "seq2seq", "host"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["tiny-infer"]
repository = "https://github.com/cj/tiny-infer"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
